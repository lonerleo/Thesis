version: '3.8'
services:
  petclinic:
    image: springcommunity/spring-petclinic-rest
    container_name: petclinic
    ports:
      - "8081:8080"
    restart: unless-stopped
    environment:
      - JAVA_OPTS=-Xmx512m
