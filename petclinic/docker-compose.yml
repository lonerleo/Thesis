# test: workflow trigger (2025-12-30, verified)
# test: trigger workflow (2025-12-30, final check)
# test: another trigger (2025-12-30)
# test: trigger workflow (2025-12-30, port fix)
# test: trigger workflow (2025-12-30)
# Trigger: test workflow run (2025-12-30)
# test: trigger workflow
version: '3.8'
# TEST COMMENT: Trigger PetClinic workflow only
# ANOTHER TEST COMMENT: Trigger PetClinic workflow and ZAP full scan
# FINAL TEST COMMENT: Workflow fix validation
services:
  petclinic:
    image: springcommunity/spring-petclinic-rest
    container_name: petclinic
    ports:
      - "8081:8080"
    restart: unless-stopped
    environment:
      - JAVA_OPTS=-Xmx512m
